<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="pos_retail.CustomerProfile">
        <Dialog size="'xl'" title="(partner ? partner.name : '') + ' — Ledger &amp; History'" bodyClass="'pos-retail-profile'">
            <!-- Header: Partner Info & Quick Tags & Action Controls -->
            <div class="pos-retail-profile__head d-flex align-items-center justify-content-between gap-3 mb-2 pb-2 border-bottom">
                <div class="d-flex align-items-center gap-2 min-w-0 flex-grow-1">
                    <div class="pos-retail-profile__avatar flex-shrink-0" t-esc="(partner ? partner.name : '?').charAt(0).toUpperCase()"/>
                    <div class="min-w-0 flex-grow-1">
                        <div class="d-flex align-items-center gap-2 flex-wrap">
                            <span class="fs-5 fw-bold text-truncate" t-esc="partner ? partner.name : ''"/>
                            <span t-if="partner and partner.membership_level_id"
                                  class="pos-retail-profile__tier"
                                  t-esc="partner.membership_level_id.name"/>
                            <span t-if="state.data and state.data.is_vendor"
                                  class="badge text-bg-warning text-dark">
                                <i class="fa fa-truck me-1"/>Vendor
                            </span>
                            <span t-if="state.data and state.data.is_customer"
                                  class="badge text-bg-info text-white">
                                <i class="fa fa-user me-1"/>Customer
                            </span>
                        </div>
                        <div class="text-muted small d-flex flex-wrap gap-3 mt-1" style="font-size: 0.8rem;">
                            <span t-if="partner and (partner.phone or (state.data and state.data.phone))">
                                <i class="fa fa-phone me-1 text-primary"/>
                                <t t-esc="state.data ? state.data.phone : partner.phone"/>
                            </span>
                            <span t-if="partner and (partner.email or (state.data and state.data.email))">
                                <i class="fa fa-envelope-o me-1 text-primary"/>
                                <t t-esc="state.data ? state.data.email : partner.email"/>
                            </span>
                            <span t-if="partner and (partner.pos_contact_address or (state.data and state.data.street))">
                                <i class="fa fa-map-marker me-1 text-primary"/>
                                <t t-esc="state.data ? (state.data.street + (state.data.city ? ', ' + state.data.city : '')) : partner.pos_contact_address"/>
                            </span>
                        </div>
                    </div>
                </div>

                <!-- Right Header Actions: Switch/Change partner & Cart sync -->
                <div class="d-flex align-items-center gap-2 flex-shrink-0">
                    <button t-if="state.activeSide === 'customer'"
                            type="button"
                            class="btn btn-sm btn-success fw-bold d-flex align-items-center gap-1 py-1 px-2 shadow-sm"
                            style="font-size: 0.78rem;"
                            t-on-click="onClickReceivePayment"
                            title="Receive Khata payment from this customer">
                        <i class="fa fa-money"/>
                        <span>Receive Payment</span>
                    </button>
                    <span t-if="isCurrentOrderPartner" class="badge text-bg-success py-1 px-2" style="font-size: 0.75rem;">
                        <i class="fa fa-check-circle me-1"/>In Current Cart
                    </span>
                    <button t-else=""
                            type="button"
                            class="btn btn-sm btn-outline-success d-flex align-items-center gap-1 py-1 px-2"
                            style="font-size: 0.78rem;"
                            t-on-click="setAsCurrentOrderCustomer"
                            title="Set this partner to active POS Cart">
                        <i class="fa fa-shopping-cart"/> Set to Cart
                    </button>
                    <div class="btn-group shadow-sm">
                        <button type="button"
                                class="btn btn-sm fw-bold d-flex align-items-center gap-1"
                                t-att-class="state.showSwitcher ? 'btn-primary text-white shadow-sm' : 'btn-outline-primary'"
                                t-on-click="toggleSwitcher"
                                title="Change or Search another Customer or Vendor">
                            <i class="fa fa-users"/>
                            <span>Change / Search</span>
                        </button>
                        <button type="button"
                                class="btn btn-outline-secondary btn-sm"
                                t-on-click="browseAllPartners"
                                title="Open Full POS Partner Directory">
                            <i class="fa fa-list"/>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Tags & Notes -->
            <div t-if="tags.length" class="pos-retail-profile__tags mb-2">
                <span t-foreach="tags" t-as="tag" t-key="tag.id"
                      class="pos-retail-profile__tag" t-esc="tag.name"/>
            </div>

            <div t-if="partner and (partner.pos_retail_note or (state.data and state.data.note))"
                 class="pos-retail-profile__note mb-2" role="note">
                <i class="fa fa-sticky-note-o me-1"/>
                <t t-esc="state.data ? state.data.note : partner.pos_retail_note"/>
            </div>

            <div t-if="overLimit" class="alert alert-danger py-2 px-3 mb-2" role="alert">
                <i class="fa fa-exclamation-triangle me-1"/>
                <strong>Over Credit Limit!</strong> This customer has exceeded their credit limit.
                Manager approval is required for credit sales.
            </div>

            <!-- IN-MODAL PARTNER SWITCHER DRAWER -->
            <div t-if="state.showSwitcher" class="pos-retail-partner-switcher border rounded-3 p-3 mb-3 bg-light shadow-sm">
                <div class="d-flex align-items-center justify-content-between mb-2 flex-wrap gap-2">
                    <div class="fw-bold text-dark d-flex align-items-center gap-2">
                        <i class="fa fa-users text-primary"/>
                        <span>Change Customer or Vendor in Ledger</span>
                    </div>
                    <!-- Type Filter Pills -->
                    <div class="btn-group btn-group-sm" role="group">
                        <button type="button"
                                class="btn"
                                t-att-class="state.filterType === 'all' ? 'btn-primary' : 'btn-outline-secondary'"
                                t-on-click="() => this.setFilterType('all')">
                            All
                        </button>
                        <button type="button"
                                class="btn"
                                t-att-class="state.filterType === 'customer' ? 'btn-primary' : 'btn-outline-secondary'"
                                t-on-click="() => this.setFilterType('customer')">
                            Customers
                        </button>
                        <button type="button"
                                class="btn"
                                t-att-class="state.filterType === 'vendor' ? 'btn-warning text-dark' : 'btn-outline-secondary'"
                                t-on-click="() => this.setFilterType('vendor')">
                            Vendors
                        </button>
                    </div>
                </div>

                <div class="input-group mb-2">
                    <span class="input-group-text bg-white"><i class="fa fa-search text-muted"/></span>
                    <input type="text"
                           class="form-control"
                           placeholder="Search customer/vendor by name, phone, mobile, email, or reference..."
                           t-model="state.searchQuery"
                           t-on-keydown="onSearchKeydown"
                           autofocus="autofocus"/>
                    <button class="btn btn-outline-primary"
                            type="button"
                            t-att-disabled="state.searchingServer"
                            t-on-click="searchServerPartners"
                            title="Search database server for partners not loaded in POS">
                        <i t-att-class="state.searchingServer ? 'fa fa-circle-o-notch fa-spin' : 'fa fa-database'"/>
                        <span class="ms-1 d-none d-sm-inline">Server Search</span>
                    </button>
                    <button class="btn btn-outline-secondary"
                            type="button"
                            t-on-click="toggleSwitcher"
                            title="Close Switcher">
                        <i class="fa fa-times"/>
                    </button>
                </div>

                <!-- Results list -->
                <div class="pos-retail-switcher-results border rounded bg-white">
                    <div t-if="!matchingPartners.length" class="text-center text-muted py-3 small">
                        <div t-if="state.searchQuery">
                            No matching partners found locally.
                            <a href="#" class="fw-bold text-primary ms-1" t-on-click.prevent="searchServerPartners">
                                Search database server?
                            </a>
                        </div>
                        <div t-else="">
                            Type name, phone or code to filter, or click Browse Directory.
                        </div>
                    </div>
                    <div t-else="" class="list-group list-group-flush" style="max-height: 220px; overflow-y: auto;">
                        <button t-foreach="matchingPartners" t-as="p" t-key="p.id"
                                type="button"
                                class="list-group-item list-group-item-action d-flex align-items-center justify-content-between py-2 px-3 gap-2"
                                t-att-class="{ 'active bg-primary-subtle text-dark border-primary': p.id === partner.id }"
                                t-on-click="() => this.selectPartner(p)">
                            <div class="d-flex align-items-center gap-2 min-w-0">
                                <div class="rounded-circle bg-secondary text-white d-flex align-items-center justify-content-center flex-shrink-0"
                                     style="width: 32px; height: 32px; font-size: 0.85rem; font-weight: bold;"
                                     t-esc="(p.name || '?').charAt(0).toUpperCase()"/>
                                <div class="min-w-0 text-start">
                                    <div class="fw-bold text-truncate" t-esc="p.name"/>
                                    <div class="small text-muted text-truncate">
                                        <span t-if="p.phone or p.mobile" class="me-2">
                                            <i class="fa fa-phone me-1"/>
                                            <t t-esc="p.phone || p.mobile"/>
                                        </span>
                                        <span t-if="p.email" class="me-2">
                                            <i class="fa fa-envelope-o me-1"/>
                                            <t t-esc="p.email"/>
                                        </span>
                                    </div>
                                </div>
                            </div>
                            <div class="d-flex align-items-center gap-1 flex-shrink-0">
                                <span t-if="p.supplier_rank and p.supplier_rank > 0"
                                      class="badge text-bg-warning text-dark" style="font-size: 0.68rem;">
                                    Vendor
                                </span>
                                <span t-if="p.customer_rank or !p.supplier_rank"
                                      class="badge text-bg-light border text-muted" style="font-size: 0.68rem;">
                                    Customer
                                </span>
                                <span t-if="p.id === partner.id"
                                      class="badge text-bg-primary ms-1" style="font-size: 0.72rem;">
                                    <i class="fa fa-check me-1"/>Active
                                </span>
                                <span t-else="" class="btn btn-sm btn-outline-primary py-0 px-2" style="font-size: 0.75rem;">
                                    Select
                                </span>
                            </div>
                        </button>
                    </div>
                </div>
            </div>

            <!-- PRIMARY NAVIGATION: SIDE-BY-SIDE TABS FOR CUSTOMER AND VENDOR -->
            <div class="pos-retail-main-tabs btn-group w-100 mb-2" role="group">
                <button type="button"
                        class="btn fw-bold d-flex align-items-center justify-content-center gap-2 py-1"
                        t-att-class="state.activeSide === 'customer' ? 'btn-primary' : 'btn-outline-primary'"
                        t-on-click="() => this.setActiveSide('customer')">
                    <i class="fa fa-user-circle fs-6"/>
                    <span>Customer Details &amp; Ledger</span>
                    <span t-if="state.data and state.data.sales_count" class="badge text-bg-light ms-1">
                        <t t-esc="state.data.sales_count"/> Orders
                    </span>
                </button>
                <button type="button"
                        class="btn fw-bold d-flex align-items-center justify-content-center gap-2 py-1"
                        t-att-class="state.activeSide === 'vendor' ? 'btn-warning text-dark' : 'btn-outline-warning text-dark'"
                        t-on-click="() => this.setActiveSide('vendor')">
                    <i class="fa fa-truck fs-6"/>
                    <span>Vendor Details &amp; Ledger</span>
                    <span t-if="state.data and (state.data.purchase_orders_count or (state.data.vendor_bills and state.data.vendor_bills.length))" class="badge text-bg-dark ms-1">
                        <t t-esc="state.data.purchase_orders_count || state.data.vendor_bills.length"/> Records
                    </span>
                </button>
            </div>

            <!-- Loading Spinner (Non-blocking) -->
            <div t-if="state.loading" class="text-center p-3 text-muted">
                <i class="fa fa-circle-o-notch fa-spin fa-2x"/>
                <div class="mt-1 small">Fetching live ledger and transaction history…</div>
            </div>

            <!-- Offline Warning Banner -->
            <div t-elif="state.failed" class="alert alert-warning py-2 px-3 mb-2" role="alert">
                <i class="fa fa-wifi me-1"/>
                <strong>Offline Mode:</strong> Showing cached profile figures. Live transaction history requires connection.
            </div>

            <!-- ======================================================== -->
            <!-- TAB 1: CUSTOMER VIEW                                      -->
            <!-- ======================================================== -->
            <div t-if="state.activeSide === 'customer'" class="pos-retail-tab-content">
                <!-- ================= CUSTOMER SUMMARY CARD (Requirement 6) ================= -->
                <div class="pos-retail-customer-summary-card card border mb-3 shadow-sm">
                    <div class="card-header bg-light py-2 px-3 d-flex align-items-center justify-content-between">
                        <div class="fw-bold small text-dark d-flex align-items-center gap-2">
                            <i class="fa fa-id-card-o text-primary"/>
                            <span>Customer Financial Summary &amp; Credit Status</span>
                        </div>
                        <button type="button"
                                class="btn btn-sm btn-success fw-bold d-flex align-items-center gap-1 py-1 px-3 shadow-sm"
                                style="font-size: 0.78rem;"
                                t-on-click="onClickReceivePayment"
                                title="Receive Khata payment from this customer">
                            <i class="fa fa-money"/>
                            <span>Receive Payment</span>
                        </button>
                    </div>
                    <div class="card-body p-2">
                        <div class="row g-2">
                            <!-- 1. Total Credit / Sales -->
                            <div class="col-6 col-md-4">
                                <div class="p-2 border rounded bg-light h-100">
                                    <div class="text-muted small text-uppercase fw-semibold" style="font-size: 0.7rem;">1. Total Credit / Sales</div>
                                    <div class="fs-6 fw-bold text-primary"
                                         t-esc="state.data ? (state.data.total_sales_formatted || state.data.customer_debit.formatted) : formatCurrency(partner.pos_total_spent)"/>
                                    <div class="small text-muted" style="font-size: 0.72rem;">All orders &amp; debit charges</div>
                                </div>
                            </div>
                            <!-- 2. Total Paid -->
                            <div class="col-6 col-md-4">
                                <div class="p-2 border rounded bg-light h-100">
                                    <div class="text-muted small text-uppercase fw-semibold" style="font-size: 0.7rem;">2. Total Paid</div>
                                    <div class="fs-6 fw-bold text-success"
                                         t-esc="state.data ? (state.data.total_paid_formatted || state.data.customer_credit.formatted) : formatCurrency((partner.pos_total_spent || 0) - (partner.pos_outstanding_balance || 0))"/>
                                    <div class="small text-muted" style="font-size: 0.72rem;">Total receipts &amp; payments</div>
                                </div>
                            </div>
                            <!-- 3. Total Outstanding -->
                            <div class="col-6 col-md-4">
                                <div class="p-2 border rounded h-100"
                                     t-att-class="(state.data ? state.data.customer_balance.amount : partner.pos_outstanding_balance) > 0 ? 'bg-danger-subtle border-danger' : 'bg-success-subtle border-success'">
                                    <div class="d-flex align-items-center justify-content-between">
                                        <span class="text-muted small text-uppercase fw-semibold" style="font-size: 0.7rem;">3. Total Outstanding</span>
                                        <span t-if="(state.data ? state.data.customer_balance.amount : partner.pos_outstanding_balance) > 0"
                                              class="badge text-bg-danger" style="font-size: 0.65rem;">Owed</span>
                                        <span t-else="" class="badge text-bg-success" style="font-size: 0.65rem;">Clear</span>
                                    </div>
                                    <div class="fs-6 fw-bold"
                                         t-att-class="(state.data ? state.data.customer_balance.amount : partner.pos_outstanding_balance) > 0 ? 'text-danger' : 'text-success'"
                                         t-esc="state.data ? (state.data.total_outstanding_formatted || state.data.customer_balance.formatted) : formatCurrency(partner.pos_outstanding_balance)"/>
                                    <div class="small text-muted" style="font-size: 0.72rem;">Across all unpaid transactions</div>
                                </div>
                            </div>
                            <!-- 4. Outstanding Invoices Count -->
                            <div class="col-6 col-md-4">
                                <div class="p-2 border rounded bg-light h-100">
                                    <div class="text-muted small text-uppercase fw-semibold" style="font-size: 0.7rem;">4. Outstanding Invoices</div>
                                    <div class="fs-6 fw-bold text-dark">
                                        <t t-esc="state.data ? (state.data.outstanding_invoices_count || 0) : 0"/>
                                        <span class="fs-6 fw-normal text-muted ms-1" style="font-size: 0.75rem;">unpaid / partial</span>
                                    </div>
                                    <div class="small text-muted" style="font-size: 0.72rem;">Pending invoices &amp; orders</div>
                                </div>
                            </div>
                            <!-- 5. Oldest Outstanding -->
                            <div class="col-6 col-md-4">
                                <div class="p-2 border rounded bg-light h-100">
                                    <div class="text-muted small text-uppercase fw-semibold" style="font-size: 0.7rem;">5. Oldest Outstanding</div>
                                    <div class="fw-bold text-dark text-truncate" style="font-size: 0.85rem;"
                                         t-esc="(state.data and state.data.oldest_outstanding_name) ? state.data.oldest_outstanding_name : '—'"/>
                                    <div class="small text-muted" style="font-size: 0.72rem;">
                                        <t t-if="state.data and state.data.oldest_outstanding_date">
                                            <i class="fa fa-calendar me-1"/>Date: <t t-esc="state.data.oldest_outstanding_date"/>
                                        </t>
                                        <t t-else="">No open debt</t>
                                    </div>
                                </div>
                            </div>
                            <!-- 6. Latest Transaction -->
                            <div class="col-6 col-md-4">
                                <div class="p-2 border rounded bg-light h-100">
                                    <div class="text-muted small text-uppercase fw-semibold" style="font-size: 0.7rem;">6. Latest Transaction</div>
                                    <div class="fw-bold text-dark text-truncate" style="font-size: 0.85rem;"
                                         t-esc="(state.data and state.data.latest_transaction_name) ? state.data.latest_transaction_name : '—'"/>
                                    <div class="small text-muted" style="font-size: 0.72rem;">
                                        <t t-if="state.data and state.data.latest_transaction_date">
                                            <i class="fa fa-clock-o me-1"/><t t-esc="state.data.latest_transaction_date"/> (<t t-esc="state.data.latest_transaction_amount || ''"/>)
                                        </t>
                                        <t t-else="">No prior history</t>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>


                <!-- Repeat Last Order Banner -->
                <div t-if="state.data and state.data.last_basket and state.data.last_basket.length"
                     class="pos-retail-history__last d-flex align-items-center justify-content-between gap-2 mb-2 py-1 px-2 border rounded">
                    <div class="d-flex align-items-center gap-2 min-w-0 flex-grow-1" style="font-size: 0.82rem;">
                        <i class="fa fa-shopping-basket text-primary flex-shrink-0"/>
                        <span class="fw-bold flex-shrink-0">
                            Last: <span class="text-primary" t-esc="state.data.last_order_name"/>
                            (<span class="text-success fw-bold" t-esc="state.data.last_order_total"/>)
                        </span>
                        <span class="text-muted text-truncate" style="max-width: 500px;">
                            <t t-foreach="state.data.last_basket" t-as="item" t-key="item.product_id">
                                <span t-esc="item.name"/> (x<t t-esc="item.qty"/>)<t t-if="!item_last">, </t>
                            </t>
                        </span>
                    </div>
                    <button class="btn btn-sm btn-primary py-0 px-2 flex-shrink-0"
                            style="font-size: 0.78rem; height: 26px;"
                            t-on-click="() => this.repeatLastOrder()"
                            title="Load items from last order into cart">
                        <i class="fa fa-repeat me-1"/> Repeat Order
                    </button>
                </div>

                <!-- Customer History Sub-Tabs -->
                <div class="pos-retail-sub-tabs d-flex flex-wrap gap-1 mb-2">
                    <button class="btn btn-sm"
                            t-att-class="state.customerTab === 'sales' ? 'btn-primary' : 'btn-outline-secondary'"
                            t-on-click="() => this.setCustomerTab('sales')">
                        <i class="fa fa-shopping-cart me-1"/> Sales &amp; Orders
                        <span t-if="state.data" class="badge text-bg-light ms-1" t-esc="state.data.sales_count"/>
                    </button>
                    <button class="btn btn-sm"
                            t-att-class="state.customerTab === 'payments' ? 'btn-primary' : 'btn-outline-secondary'"
                            t-on-click="() => this.setCustomerTab('payments')">
                        <i class="fa fa-credit-card me-1"/> Payments Received
                        <span t-if="state.data and state.data.payments" class="badge text-bg-light ms-1" t-esc="state.data.payments.length"/>
                    </button>
                    <button class="btn btn-sm"
                            t-att-class="state.customerTab === 'open' ? 'btn-primary' : 'btn-outline-secondary'"
                            t-on-click="() => this.setCustomerTab('open')">
                        <i class="fa fa-clock-o me-1"/> Unpaid Invoices
                        <span t-if="state.data and state.data.open_invoices" class="badge text-bg-danger text-white ms-1" t-esc="state.data.open_invoices.length"/>
                    </button>
                    <button class="btn btn-sm"
                            t-att-class="state.customerTab === 'purchases' ? 'btn-primary' : 'btn-outline-secondary'"
                            t-on-click="() => this.setCustomerTab('purchases')">
                        <i class="fa fa-star me-1"/> Buys Often
                        <span t-if="state.data and state.data.top_products" class="badge text-bg-light ms-1" t-esc="state.data.top_products.length"/>
                    </button>
                    <button class="btn btn-sm"
                            t-att-class="state.customerTab === 'credit' ? 'btn-primary' : 'btn-outline-secondary'"
                            t-on-click="() => this.setCustomerTab('credit')">
                        <i class="fa fa-book me-1"/> Credit Sales (Khata)
                        <span t-if="state.data and state.data.credit_sales" class="badge text-bg-light ms-1" t-esc="state.data.credit_sales.length"/>
                    </button>
                    <button class="btn btn-sm"
                            t-att-class="state.customerTab === 'refunds' ? 'btn-primary' : 'btn-outline-secondary'"
                            t-on-click="() => this.setCustomerTab('refunds')">
                        <i class="fa fa-undo me-1"/> Returns
                        <span t-if="state.data and state.data.refunds_count" class="badge text-bg-light ms-1" t-esc="state.data.refunds_count"/>
                    </button>
                    <button class="btn btn-sm"
                            t-att-class="state.customerTab === 'quotations' ? 'btn-primary' : 'btn-outline-secondary'"
                            t-on-click="() => this.setCustomerTab('quotations')">
                        <i class="fa fa-file-text-o me-1"/> Quotations
                        <span t-if="state.data and state.data.quotations" class="badge text-bg-light ms-1" t-esc="state.data.quotations.length"/>
                    </button>
                </div>

                <!-- Customer Sub-Tab Content -->
                <div class="pos-retail-history__body border rounded p-2 bg-light">
                    <!-- 1. Sales & Orders -->
                    <t t-if="state.customerTab === 'sales'">
                        <div t-if="!state.data or !state.data.sales.length" class="text-muted p-3 text-center">
                            No sales orders recorded for this customer.
                        </div>
                        <table t-else="" class="table table-sm table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Date</th>
                                    <th>Receipt No.</th>
                                    <th>Invoice</th>
                                    <th>Cashier</th>
                                    <th class="text-end">Total</th>
                                    <th class="text-end">Paid</th>
                                    <th class="text-end">Balance</th>
                                    <th class="text-center">Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="state.data.sales" t-as="row" t-key="row.id">
                                    <td class="small" t-esc="row.date"/>
                                    <td class="fw-bold" t-esc="row.receipt_number"/>
                                    <td class="small">
                                        <span t-if="row.invoice" class="badge text-bg-light" t-esc="row.invoice"/>
                                        <span t-else="" class="text-muted">-</span>
                                    </td>
                                    <td class="small" t-esc="row.cashier"/>
                                    <td class="text-end fw-bold" t-esc="row.amount_formatted"/>
                                    <td class="text-end text-success" t-esc="row.paid_amount_formatted"/>
                                    <td class="text-end fw-bold"
                                        t-att-class="row.balance_amount > 0 ? 'text-danger' : 'text-muted'"
                                        t-esc="row.balance_amount_formatted"/>
                                    <td class="text-center">
                                        <span t-att-class="row.payment_status === 'paid' ? 'badge text-bg-success' :
                                                           row.payment_status === 'partial' ? 'badge text-bg-warning text-dark' :
                                                           'badge text-bg-danger'"
                                              t-esc="row.payment_status_label"/>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </t>

                    <!-- 2. Payments Received -->
                    <t t-if="state.customerTab === 'payments'">
                        <div t-if="!state.data or !state.data.payments.length" class="text-muted p-3 text-center">
                            No payments recorded yet.
                        </div>
                        <table t-else="" class="table table-sm table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Date</th>
                                    <th>Receipt / Ref</th>
                                    <th>Description</th>
                                    <th class="text-end">Amount Paid</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="state.data.payments" t-as="row" t-key="row.id">
                                    <td class="small" t-esc="row.date"/>
                                    <td class="fw-bold" t-esc="row.receipt_number || row.ref"/>
                                    <td class="small text-muted" t-esc="row.label"/>
                                    <td class="text-end fw-bold text-success" t-esc="row.amount_formatted"/>
                                </tr>
                            </tbody>
                        </table>
                    </t>

                    <!-- 3. Unpaid Invoices -->
                    <t t-if="state.customerTab === 'open'">
                        <div t-if="!state.data or !state.data.open_invoices.length" class="text-muted p-3 text-center text-success">
                            <i class="fa fa-check-circle me-1"/> Nothing outstanding. This customer is settled up!
                        </div>
                        <table t-else="" class="table table-sm table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Date</th>
                                    <th>Receipt / Ref</th>
                                    <th>Description</th>
                                    <th class="text-end">Total Billed</th>
                                    <th class="text-end">Still Unpaid</th>
                                    <th class="text-center">Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="state.data.open_invoices" t-as="row" t-key="row.id">
                                    <td class="small" t-esc="row.date"/>
                                    <td class="fw-bold" t-esc="row.receipt_number || row.ref"/>
                                    <td class="small text-muted" t-esc="row.label"/>
                                    <td class="text-end" t-esc="row.amount_formatted"/>
                                    <td class="text-end fw-bold text-danger" t-esc="row.residual_formatted"/>
                                    <td class="text-center">
                                        <span class="badge text-bg-danger" t-esc="row.payment_status_label"/>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </t>

                    <!-- 4. Buys Often -->
                    <t t-if="state.customerTab === 'purchases'">
                        <div t-if="!state.data or !state.data.top_products.length" class="text-muted p-3 text-center">
                            No product history available.
                        </div>
                        <table t-else="" class="table table-sm table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Product</th>
                                    <th class="text-end">Visits / Times</th>
                                    <th class="text-end">Quantity Bought</th>
                                    <th class="text-end">Total Spent</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="state.data.top_products" t-as="p" t-key="p.name">
                                    <td class="fw-bold" t-esc="p.name"/>
                                    <td class="text-end" t-esc="p.times"/>
                                    <td class="text-end" t-esc="p.qty"/>
                                    <td class="text-end fw-bold text-primary" t-esc="p.spent_formatted"/>
                                </tr>
                            </tbody>
                        </table>
                    </t>

                    <!-- 5. Credit Sales -->
                    <t t-if="state.customerTab === 'credit'">
                        <div t-if="!state.data or !state.data.credit_sales.length" class="text-muted p-3 text-center">
                            No credit sales recorded for this customer.
                        </div>
                        <table t-else="" class="table table-sm table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Date</th>
                                    <th>Receipt No.</th>
                                    <th>Cashier</th>
                                    <th class="text-end">Total</th>
                                    <th class="text-end">Paid</th>
                                    <th class="text-end">Balance</th>
                                    <th class="text-center">Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="state.data.credit_sales" t-as="row" t-key="row.id">
                                    <td class="small" t-esc="row.date"/>
                                    <td class="fw-bold" t-esc="row.receipt_number"/>
                                    <td class="small" t-esc="row.cashier"/>
                                    <td class="text-end fw-bold" t-esc="row.amount_formatted"/>
                                    <td class="text-end text-success" t-esc="row.paid_amount_formatted"/>
                                    <td class="text-end fw-bold"
                                        t-att-class="row.balance_amount > 0 ? 'text-danger' : 'text-muted'"
                                        t-esc="row.balance_amount_formatted"/>
                                    <td class="text-center">
                                        <span t-att-class="row.payment_status === 'paid' ? 'badge text-bg-success' :
                                                           row.payment_status === 'partial' ? 'badge text-bg-warning text-dark' :
                                                           'badge text-bg-danger'"
                                              t-esc="row.payment_status_label"/>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </t>

                    <!-- 6. Returns / Refunds -->
                    <t t-if="state.customerTab === 'refunds'">
                        <div t-if="!state.data or !state.data.refunds.length" class="text-muted p-3 text-center">
                            No return or refund orders on file.
                        </div>
                        <table t-else="" class="table table-sm table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Date</th>
                                    <th>Receipt No.</th>
                                    <th>Cashier</th>
                                    <th class="text-end">Refunded Amount</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="state.data.refunds" t-as="row" t-key="row.id">
                                    <td class="small" t-esc="row.date"/>
                                    <td class="fw-bold text-danger" t-esc="row.receipt_number"/>
                                    <td class="small" t-esc="row.cashier"/>
                                    <td class="text-end fw-bold text-danger" t-esc="row.amount_formatted"/>
                                </tr>
                            </tbody>
                        </table>
                    </t>

                    <!-- 7. Quotations -->
                    <t t-if="state.customerTab === 'quotations'">
                        <div t-if="!state.data or !state.data.quotations.length" class="text-muted p-3 text-center">
                            No open quotations found.
                        </div>
                        <table t-else="" class="table table-sm table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Date</th>
                                    <th>Quotation Ref</th>
                                    <th>Status</th>
                                    <th class="text-end">Total Amount</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="state.data.quotations" t-as="q" t-key="q.name">
                                    <td class="small" t-esc="q.date"/>
                                    <td class="fw-bold" t-esc="q.receipt_number"/>
                                    <td><span class="badge text-bg-info text-white" t-esc="q.state_label || q.state"/></td>
                                    <td class="text-end fw-bold" t-esc="q.amount_formatted"/>
                                </tr>
                            </tbody>
                        </table>
                    </t>
                </div>
            </div>

            <!-- ======================================================== -->
            <!-- TAB 2: VENDOR VIEW                                        -->
            <!-- ======================================================== -->
            <div t-if="state.activeSide === 'vendor'" class="pos-retail-tab-content">
                <!-- VENDOR KHATA / LEDGER KPI CARDS -->
                <div class="pos-retail-profile__grid mb-2">
                    <!-- 1. Total Bills (Vendor Credit) -->
                    <div class="pos-retail-profile__stat is-debit" title="Goods &amp; services billed by vendor">
                        <span class="pos-retail-profile__label text-primary">Total Bills (Credit)</span>
                        <span class="pos-retail-profile__value text-primary"
                              t-esc="state.data ? state.data.vendor_bills_total.formatted : '$ 0.00'"/>
                    </div>

                    <!-- 2. Total Paid (Vendor Debit) -->
                    <div class="pos-retail-profile__stat is-credit" title="Payments made to this vendor">
                        <span class="pos-retail-profile__label text-success">Total Paid (Debit)</span>
                        <span class="pos-retail-profile__value text-success"
                              t-esc="state.data ? state.data.vendor_paid_total.formatted : '$ 0.00'"/>
                    </div>

                    <!-- 3. Net Payable Balance -->
                    <div class="pos-retail-profile__stat"
                         t-att-class="(state.data and state.data.vendor_balance.amount > 0) ? 'is-danger' : 'is-success'">
                        <div class="d-flex align-items-center justify-content-between gap-1">
                            <span class="pos-retail-profile__label">Net Payable</span>
                            <span t-if="state.data and state.data.vendor_balance.amount > 0"
                                  class="badge text-bg-warning text-dark" style="font-size: 0.65rem; padding: 0.15rem 0.35rem;">
                                We Owe
                            </span>
                            <span t-else="" class="badge text-bg-success" style="font-size: 0.65rem; padding: 0.15rem 0.35rem;">
                                Clear
                            </span>
                        </div>
                        <span class="pos-retail-profile__value"
                              t-esc="state.data ? state.data.vendor_balance.formatted : '$ 0.00'"/>
                    </div>

                    <!-- 4. Purchase Orders Count -->
                    <div class="pos-retail-profile__stat">
                        <div class="d-flex align-items-center justify-content-between gap-1">
                            <span class="pos-retail-profile__label">Purchase Orders</span>
                            <span t-if="state.data and state.data.is_vendor"
                                  class="badge text-bg-success" style="font-size: 0.65rem; padding: 0.15rem 0.35rem;">
                                Supplier
                            </span>
                        </div>
                        <span class="pos-retail-profile__value"
                              t-esc="(state.data ? state.data.purchase_orders_count : 0) + ' Orders'"/>
                    </div>
                </div>

                <!-- Vendor History Sub-Tabs -->
                <div class="pos-retail-sub-tabs d-flex flex-wrap gap-1 mb-2">
                    <button class="btn btn-sm"
                            t-att-class="state.vendorTab === 'purchase_orders' ? 'btn-warning text-dark' : 'btn-outline-secondary'"
                            t-on-click="() => this.setVendorTab('purchase_orders')">
                        <i class="fa fa-file-text-o me-1"/> Purchase Orders (POs)
                        <span t-if="state.data and state.data.purchase_orders" class="badge text-bg-light ms-1" t-esc="state.data.purchase_orders.length"/>
                    </button>
                    <button class="btn btn-sm"
                            t-att-class="state.vendorTab === 'vendor_bills' ? 'btn-warning text-dark' : 'btn-outline-secondary'"
                            t-on-click="() => this.setVendorTab('vendor_bills')">
                        <i class="fa fa-file-invoice me-1"/> Vendor Bills
                        <span t-if="state.data and state.data.vendor_bills" class="badge text-bg-light ms-1" t-esc="state.data.vendor_bills.length"/>
                    </button>
                    <button class="btn btn-sm"
                            t-att-class="state.vendorTab === 'vendor_payments' ? 'btn-warning text-dark' : 'btn-outline-secondary'"
                            t-on-click="() => this.setVendorTab('vendor_payments')">
                        <i class="fa fa-money me-1"/> Payments Made
                        <span t-if="state.data and state.data.vendor_payments" class="badge text-bg-light ms-1" t-esc="state.data.vendor_payments.length"/>
                    </button>
                    <button class="btn btn-sm"
                            t-att-class="state.vendorTab === 'unpaid_bills' ? 'btn-warning text-dark' : 'btn-outline-secondary'"
                            t-on-click="() => this.setVendorTab('unpaid_bills')">
                        <i class="fa fa-exclamation-circle me-1"/> Unpaid Bills
                        <span t-if="state.data and state.data.unpaid_bills" class="badge text-bg-danger text-white ms-1" t-esc="state.data.unpaid_bills.length"/>
                    </button>
                </div>

                <!-- Vendor Sub-Tab Content -->
                <div class="pos-retail-history__body border rounded p-2 bg-light">
                    <!-- 1. Purchase Orders -->
                    <t t-if="state.vendorTab === 'purchase_orders'">
                        <div t-if="!state.data or !state.data.purchase_orders.length" class="text-muted p-3 text-center">
                            No purchase orders found for this vendor.
                        </div>
                        <table t-else="" class="table table-sm table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Date</th>
                                    <th>PO Reference</th>
                                    <th>Status</th>
                                    <th class="text-end">Amount</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="state.data.purchase_orders" t-as="po" t-key="po.id">
                                    <td class="small" t-esc="po.date"/>
                                    <td class="fw-bold" t-esc="po.receipt_number"/>
                                    <td>
                                        <span t-att-class="po.state === 'purchase' ? 'badge text-bg-success' :
                                                           po.state === 'done' ? 'badge text-bg-secondary' :
                                                           po.state === 'cancel' ? 'badge text-bg-danger' :
                                                           'badge text-bg-warning text-dark'"
                                              t-esc="po.state_label || po.state"/>
                                    </td>
                                    <td class="text-end fw-bold" t-esc="po.amount_formatted"/>
                                </tr>
                            </tbody>
                        </table>
                    </t>

                    <!-- 2. Vendor Bills -->
                    <t t-if="state.vendorTab === 'vendor_bills'">
                        <div t-if="!state.data or !state.data.vendor_bills.length" class="text-muted p-3 text-center">
                            No vendor bills recorded.
                        </div>
                        <table t-else="" class="table table-sm table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Date</th>
                                    <th>Bill Reference</th>
                                    <th>Description</th>
                                    <th class="text-end">Total Billed</th>
                                    <th class="text-end">Paid</th>
                                    <th class="text-end">Still Owed</th>
                                    <th class="text-center">Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="state.data.vendor_bills" t-as="row" t-key="row.id">
                                    <td class="small" t-esc="row.date"/>
                                    <td class="fw-bold" t-esc="row.receipt_number"/>
                                    <td class="small text-muted" t-esc="row.label"/>
                                    <td class="text-end fw-bold" t-esc="row.amount_formatted"/>
                                    <td class="text-end text-success" t-esc="row.paid_amount_formatted"/>
                                    <td class="text-end fw-bold text-danger" t-esc="row.residual_formatted"/>
                                    <td class="text-center">
                                        <span t-att-class="row.payment_status === 'paid' ? 'badge text-bg-success' :
                                                           row.payment_status === 'partial' ? 'badge text-bg-warning text-dark' :
                                                           'badge text-bg-danger'"
                                              t-esc="row.payment_status_label"/>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </t>

                    <!-- 3. Payments Made to Vendor -->
                    <t t-if="state.vendorTab === 'vendor_payments'">
                        <div t-if="!state.data or !state.data.vendor_payments.length" class="text-muted p-3 text-center">
                            No payments made to this vendor yet.
                        </div>
                        <table t-else="" class="table table-sm table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Date</th>
                                    <th>Payment Ref</th>
                                    <th>Description</th>
                                    <th class="text-end">Amount Paid</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="state.data.vendor_payments" t-as="row" t-key="row.id">
                                    <td class="small" t-esc="row.date"/>
                                    <td class="fw-bold" t-esc="row.receipt_number"/>
                                    <td class="small text-muted" t-esc="row.label"/>
                                    <td class="text-end fw-bold text-success" t-esc="row.amount_formatted"/>
                                </tr>
                            </tbody>
                        </table>
                    </t>

                    <!-- 4. Unpaid Vendor Bills -->
                    <t t-if="state.vendorTab === 'unpaid_bills'">
                        <div t-if="!state.data or !state.data.unpaid_bills.length" class="text-muted p-3 text-center text-success">
                            <i class="fa fa-check-circle me-1"/> All bills from this vendor are settled!
                        </div>
                        <table t-else="" class="table table-sm table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Date</th>
                                    <th>Bill Reference</th>
                                    <th>Description</th>
                                    <th class="text-end">Total Bill</th>
                                    <th class="text-end">Still Owed</th>
                                    <th class="text-center">Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="state.data.unpaid_bills" t-as="row" t-key="row.id">
                                    <td class="small" t-esc="row.date"/>
                                    <td class="fw-bold" t-esc="row.receipt_number"/>
                                    <td class="small text-muted" t-esc="row.label"/>
                                    <td class="text-end" t-esc="row.amount_formatted"/>
                                    <td class="text-end fw-bold text-danger" t-esc="row.residual_formatted"/>
                                    <td class="text-center">
                                        <span class="badge text-bg-danger" t-esc="row.payment_status_label"/>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </t>
                </div>
            </div>

            <!-- Modal Footer -->
            <t t-set-slot="footer">
                <div class="d-flex justify-content-between align-items-center w-100 gap-2 flex-wrap">
                    <div class="d-flex align-items-center gap-2">
                        <button type="button"
                                class="btn btn-outline-primary"
                                t-on-click="toggleSwitcher">
                            <i class="fa fa-users me-1"/>
                            <t t-if="state.showSwitcher">Hide Switcher</t>
                            <t t-else="">Change Customer / Vendor</t>
                        </button>
                        <button type="button"
                                class="btn btn-outline-secondary"
                                t-on-click="browseAllPartners"
                                title="Open Full POS Partner Directory">
                            <i class="fa fa-list me-1"/>Directory
                        </button>
                    </div>
                    <div class="d-flex align-items-center gap-2">
                        <button t-if="state.activeSide === 'customer'"
                                type="button"
                                class="btn btn-success fw-bold d-flex align-items-center gap-1 px-3"
                                t-on-click="onClickReceivePayment"
                                title="Receive Khata payment from this customer">
                            <i class="fa fa-money me-1"/>
                            <span>Receive Payment</span>
                        </button>
                        <button type="button" class="btn btn-secondary px-4" t-on-click="() => this.props.close()">
                            Close
                        </button>
                    </div>
                </div>
            </t>
        </Dialog>
    </t>

</templates>

